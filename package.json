{
  "name": "portfolio",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "dev": "concurrently \"npm run backend\" \"npm run frontend\"",
    "build": "cd frontend && npm run build --verbose&& cd ../backend && npm start --verbose",
    "frontend": "cd frontend && npm start --verbose",
    "backend": "cd backend && npm run dev --verbose",
    "update": "cd frontend && npm install --verbose && cd ../backend && npm install --verbose && cd .. && npm install --verbose",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "daFinndus",
  "license": "ISC",
  "description": "This is my portfolio",
  "dependencies": {
    "concurrently": "^8.2.2"
  }
}