{
  "name": "portfolio",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "dev": "concurrently \"npm run backend\" \"npm run frontend\"",
    "build": "cd frontend && npm run build && cd ../backend && npm start",
    "frontend": "cd frontend && npm start",
    "backend": "cd backend && npm run dev",
    "update": "cd frontend && npm install --verbose -y && cd ../backend && npm install --verbose -y && cd .. && npm install --verbose -y",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "daFinndus",
  "license": "ISC",
  "description": "This is my portfolio",
  "dependencies": {
    "concurrently": "^8.2.2"
  }
}