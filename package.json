{
  "name": "portfolio",
  "version": "1.0.0",
  "main": "./backend/build.js",
  "scripts": {
    "app": "cd frontend && npm run build --verbose && cd ../backend && npm start --verbose",
    "start-build": "cd backend && npm run start --verbose",
    "build": "cd frontend && npm run build --verbose",
    "install-packages": "cd frontend && npm i --verbose && cd ../backend && npm i --verbose && cd .. && npm i --verbose"
  },
  "author": "Finn Luca Jensen",
  "license": "ISC",
  "description": "This is my portfolio"
}